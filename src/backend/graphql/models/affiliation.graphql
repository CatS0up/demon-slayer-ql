type Affiliation {
    _id: ID!
    name: String!
    mainAffilation: Affiliation @belongsTo
}

extend type Query {
    affiliations: [Affiliation!]! @all
    affiliation:(_id: ID @eq): Affiliation @find
}
